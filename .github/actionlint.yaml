# GitHub runner scale-set selectors managed by the distributed runner farm.
# Keep this list aligned with the organization pool catalog so actionlint can
# validate workflows that use a single scale-set routing label.
self-hosted-runner:
  labels:
    - ci-pool-rust
    - ci-pool-python
    - ci-pool-typescript
    - ci-pool-go
    - ci-pool-ops
    - ci-pool-system
    - ci-pool-residential-egress
