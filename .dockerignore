.git
.env
.env.*
.next
.gradle
.full-review
.omc
.codex
.agents
.worktrees
node_modules
android
# Separate apps with their own toolchains and workspace deps (Tauri device
# catalog). The Next image never imports them, and their node_modules are not
# installed in the deps stage, so leaving them in only breaks the build's
# typecheck. Same rationale as android/ above.
apps
coverage
outputs
playwright-report
storybook-static
test-results
public/codex-marketplace
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
ops/compose/production.env
ops/rendered-swag
Dockerfile
docker-compose.yaml
.dockerignore
